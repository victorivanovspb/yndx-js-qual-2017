'use strict';

import React from 'react';

export default class Submit extends React.Component {
    render() {
        return (
            <button id='submitButton' type='button' className='btn btn-primary'>
                Отправить
            </button>
        );
    }
}
